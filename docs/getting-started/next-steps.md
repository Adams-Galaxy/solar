# Next Steps

Choose the path that matches your immediate work:

- {doc}`../tutorials/system-foundations` adds component dependencies,
  contributions, and execution.
- {doc}`../concepts/result-and-errors` explains default and typed errors.
- {doc}`../concepts/lifecycle` explains ordering, rollback, reports, and stop.
- {doc}`../subsystems/index` leads directly to a subsystem.
- {doc}`../how-to/test-with-native-sim` turns an application into a repeatable
  native test.
- {doc}`../architecture/static-system` explains why Solar has a static System
  type without a runtime System object.
