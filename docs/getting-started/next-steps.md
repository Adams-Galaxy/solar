# Next Steps

Choose the path that matches your immediate work:

- {doc}`first-application` adds component dependencies and composition.
- {doc}`../concepts/result-and-errors` explains default and typed errors.
- {doc}`../concepts/components-and-ownership` explains modules and ownership.
- {doc}`../subsystems/index` leads directly to a subsystem.
- {doc}`../how-to/test-with-native-sim` turns an application into a repeatable
  native test.
- {doc}`../architecture/runtime-ownership` explains why Solar has a static
  composer without a runtime System object.
