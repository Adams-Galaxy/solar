sample:
  name: Solar system composition
  description: Explicit module ownership, dependencies, and typed adapters
common:
  platform_allow:
    - native_sim/native/64
  tags:
    - solar
    - documentation
  harness: console
  harness_config:
    type: one_line
    regex:
      - "Solar system composition passed"
tests:
  sample.solar.system_composition: {}
