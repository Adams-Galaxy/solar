sample:
  name: Solar Remote control
  description: Typed Remote Data, Action, schema, link, and session example
common:
  tags:
    - solar
    - documentation
    - remote
  integration_platforms:
    - native_sim/native/64
tests:
  sample.solar.remote_control:
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Solar Remote control passed"
